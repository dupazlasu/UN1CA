service tee_script /vendor/bin/tee.sh
    user root
    group radio
    oneshot
    disabled

on early-init
    start tee_script

on fs && property:vendor.teegris.old=true
    mount none /vendor/etc/tee_old /vendor/tee bind